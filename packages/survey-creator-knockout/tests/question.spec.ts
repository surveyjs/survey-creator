import { SurveyCreator } from "../src/creator";
//import { QuestionViewModel } from "../src/components/question";

beforeEach(() => {});

test("context actions", () => {
  /*
  const creator = new SurveyCreator();
  const questionViewModel = new QuestionViewModel(creator, <any>{ getType: () => "checkbox" })
  expect(questionViewModel.actions().length).toEqual(4);
  */
});
